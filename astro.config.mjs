// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://astwei.github.io',
  // If deploying to a subpath like https://astwei.github.io/website/
  // uncomment the next line:
  // base: '/website',
});
